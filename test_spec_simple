data {
  int N
  int_array[N] is
  int_array[N] y
}

parameters {
  real mu
  array[N] theta
  array[N] y_pot
}

model {
  for (i in 1:N) {
    theta[is[i]] ~ normal(mu, 1.0)
    y_pot[is[i]] ~ normal(theta[is[i]], 2.0)
  }
  mu ~ normal(0.0, 100.0)
}