name: Build Linux

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build (Linux x86_64)
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install C++ libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libboost-all-dev libeigen3-dev libssl-dev zlib1g-dev nlohmann-json3-dev
          git clone --depth 1 https://gitlab.com/eidheim/Simple-WebSocket-Server.git /tmp/simple-ws
          sudo mkdir -p /usr/local/include/simple-websocket-server
          sudo cp /tmp/simple-ws/*.hpp /usr/local/include/simple-websocket-server/

      - name: Set up OCaml
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: 5
          dune-cache: true
          opam-local-packages: fg_parser/*.opam

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Build
        run: |
          eval $(opam env)
          ./build.sh

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: variance-deltas-linux-x64
          path: build/
          retention-days: 3

      - name: Create release tarball
        if: startsWith(github.ref, 'refs/tags/')
        run: tar -czf variance-deltas-linux-x64.tar.gz -C build .

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            variance-deltas-linux-x64.tar.gz \
            --title "Release ${{ github.ref_name }}" \
            --prerelease
